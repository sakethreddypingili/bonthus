/** Bounds for portaled overlays — main content only (excludes sidebar + topbar). */
export const OVERLAY_CHROME_STYLE = {
  top: 'var(--app-topbar-offset, 4.5rem)',
  left: 'var(--app-sidebar-offset, 0px)',
  right: 0,
  bottom: 0,
};
